import React from 'react';
import './Home.css';

const Demo = () => {
  return (
    <div className="home-container">
      <main className="home-main">
        <section className="hero glass-card">
          <div className="hero-content">
            <h1 className="hero-title">Live Demo</h1>
            <p className="hero-subtitle">Try a demo dataset and explore the analytics dashboard — no signup required.</p>
          </div>
        </section>
      </main>
    </div>
  );
};

export default Demo;
